// ClaudeSessionInfo.swift
// Limpid — backward-compat redirect to the unified `AgentSessionInfo`
// (`Limpid/Core/Agent/AgentKind.swift`). The Claude / Codex session
// info structs were structurally identical; sub-phase 2.2a collapsed
// them into one type. Existing references to `ClaudeSessionInfo`
// still resolve via the typealias declared in `AgentKind.swift`.
