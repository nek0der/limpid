[package]
name = "limpid-agent-model"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[features]
# Fixture driver shared by every provider crate's conformance test. Off by
# default so the model stays free of file-system code in production builds.
conformance = []

[dependencies]
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
uuid.workspace = true

[lints]
workspace = true
